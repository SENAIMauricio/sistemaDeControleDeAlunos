let numeroTotal = 13; //número hipotético

for (aluno = 0; aluno <= 13; aluno++) {
    if (aluno == 0) {
        console.log("zero: " + aluno)
    } if (aluno % 2 == 0) {
        console.log("par: " + aluno)
    } else {
        console.log("ímpar: " + aluno)
    }
};

